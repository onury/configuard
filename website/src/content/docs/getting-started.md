---
title: Getting Started
description: Install and start using Configuard.
---

Welcome to the Configuard documentation. This starter page was generated by
`@onury/docs-kit` — replace it with real content.

## Installation

```bash
npm install configuard
```

## Usage

```ts
import { /* … */ } from 'configuard';
```

## Next steps

- Edit this page at `website/src/content/docs/getting-started.md`.
- Add pages under `website/src/content/docs/` and list them in the `sidebar`
  array in `website/astro.config.mjs`.
- Root markdown (CHANGELOG, `docs/*.md`) is pulled in automatically by
  `website/scripts/sync-docs.mjs` — adjust its `files` list as needed.
